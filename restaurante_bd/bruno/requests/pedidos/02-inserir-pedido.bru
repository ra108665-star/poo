meta {
  name: POST /pedidos
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/pedidos
  body: json
  auth: none
}

headers {
  Accept: application/json
  Content-Type: application/json
}

body:json {
  {
    "id": "{{$randomInt}}",
    "nome": "{{$randomLoremWord}}"
  }
}

