Exercício substituído `5`:

Ao modificar o código, tive que importar `import java.util.Arraylist`, o seu ti-
po era `Integer`, um objeto em vez de `int` que é um tipo primitivo. Na adição
de novos elementos, utiliza o método `add` em vez de fazer atribuições.

Neste novo código, não preciso me preocupar em saber o tamanho da lista.
